{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Article</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Create</button>
    {{ form_end(form) }}
{% endblock %}
